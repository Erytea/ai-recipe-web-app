# ⚠️ ВАЖНОЕ UPDATE: Требования к Python

## ❌ Проблема с Python 3.14

Ты используешь Python 3.14, которая вышла совсем недавно. Многие библиотеки (особенно `pydantic`, `aiogram`) еще не поддерживают эту версию, потому что им нужно время на тестирование и обновление.

## ✅ Решение: Установи Python 3.11 или 3.12

Тебе нужно установить Python 3.11 или 3.12 (самые стабильные версии).

###  Как установить Python 3.12 на macOS:

#### Вариант 1: Через Homebrew (рекомендуется)

```bash
# Установи Homebrew (если еще нет)
/bin/bash -c "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/HEAD/install.sh)"

# Установи Python 3.12
brew install python@3.12

# Проверь установку
python3.12 --version
```

####  Вариант 2: Скачать с сайта Python.org

1. Зайди на https://www.python.org/downloads/
2. Скачай Python 3.12.x для macOS
3. Установи как обычную программу
4. Проверь: `python3.12 --version`

---

## 🚀 После установки Python 3.12

### 1. Создай виртуальное окружение с Python 3.12

```bash
cd "/Users/Mi/ai recepie bot new"

# Удали старое виртуальное окружение
rm -rf venv

# Создай новое с Python 3.12
python3.12 -m venv venv

# Активируй его
source venv/bin/activate
```

### 2. Установи зависимости

```bash
pip install --upgrade pip
pip install -r requirements.txt
```

Теперь все должно установиться без ошибок! ✅

### 3. Создай файл .env

Скопируй `env.example` в `.env` и заполни:

```bash
cp env.example .env
nano .env  # или открой в любом редакторе
```

Вставь свои ключи:
```env
TELEGRAM_BOT_TOKEN=твой_токен_от_BotFather
OPENAI_API_KEY=твой_ключ_от_OpenAI
DATABASE_URL=sqlite://db.sqlite3
DEBUG=False
```

### 4. Запусти бота

```bash
python main.py
```

---

## 📚 Полная инструкция

Подробную инструкцию смотри в [README.md](README.md)

---

## 🆘 Если возникли проблемы

### Ошибка: "python3.12: command not found"
- Python 3.12 не установлен. Смотри инструкцию выше.

### Ошибка: "Building wheel for pydantic-core failed"
- Ты используешь Python 3.14. Перейди на Python 3.12 или 3.11.

### Ошибка: "Cannot import 'maturin'"
- Эту ошибку вызывает Python 3.14. Используй Python 3.12.

### Ошибка: "Invalid token" или "Incorrect API key"
- Проверь файл `.env` - правильно ли скопированы ключи
- Убедись, что нет лишних пробелов

---

## 💡 Почему Python 3.14 не работает?

Python 3.14 вышла в ноябре 2024 года (буквально пару недель назад!). Разработчикам библиотек нужно время:
- Протестировать совместимость
- Обновить Rust-биндинги (pydantic использует Rust для скорости)
- Выпустить новые версии

**Python 3.12** - золотой стандарт на данный момент:
- Стабильная
- Поддерживается всеми библиотеками
- Быстрая
- Безопасная

---

**После установки Python 3.12 бот заработает идеально! 🎉**


